import React = require('react');
import ReactDOM = require('react-dom');
import Panels from './Panels';

var root = document.getElementById("root");
ReactDOM.render(React.createElement(Panels), root);